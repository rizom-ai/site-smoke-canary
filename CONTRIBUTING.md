# Contributing

Keep this package minimal and content-independent. Use only public
`@rizom/brain` imports, preserve standard peer dependency metadata, and run
`bun run check` plus `npm pack --dry-run` before opening a pull request.
